services:
  mail:
    # https://hub.docker.com/r/axllent/mailpit
    # mit Alpine und ash als Shell
    image: axllent/mailpit:v1.29.7
    ports:
      # https://mailpit.axllent.org/docs/install
      # https://mailpit.axllent.org/docs/configuration
      # SMTP
      - published: 1025
        target: 1025
        protocol: tcp
      # Web UI und REST-API
      - published: 8025
        target: 8025
        protocol: tcp
    volumes:
      # https://mailpit.axllent.org/docs/configuration/email-storage
      - mailpit:/data
    environment:
      TZ: Europe/Berlin
      MP_DATABASE: /data/database.db
      # default: 500
      MP_MAX_MESSAGES: 50
    networks: [acme-network]
    hostname: mail
    container_name: mail
    cap_drop: [ALL]
    healthcheck:
      # nc = netcat
      test: ["CMD", "nc", "-z", "localhost", "1025"]
      #test: ["CMD", "wget", "--spider", "-q", "http://localhost:8025/readyz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    stop_grace_period: 3s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.5"
          memory: 512M

volumes:
  mailpit:
    name: mailpit
    external: true

networks:
  acme-network:
    name: acme-network
