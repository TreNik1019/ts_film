info:
  name: Schlagwort SPANNEND
  type: http
  seq: 9

http:
  method: GET
  url: "{{restUrl}}?spannend=true"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: spannend
      value: "true"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test('Body: Schlagwort SPANNEND und ID', () => {
          res.getBody()
            .content
            .forEach(film => {
                const { schlagwoerter, id } = film;
                expect(schlagwoerter).to.be.an('array').that.includes('SPANNEND');
                expect(id).to.be.above(0);
            });
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: matches
      value: ^application\/json(; charset=utf-8)?$
    - expression: res.body.content
      operator: isArray
    - expression: res.body.content
      operator: isNotEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
